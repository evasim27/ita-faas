$kind: http-request
description: Prijava obstoječega uporabnika. Token se shrani v spremenljivko TOKEN.
url: "{{AUTH_URL}}/identitytoolkit.googleapis.com/v1/accounts:signInWithPassword?key=fake-api-key"
method: POST
headers:
  Content-Type: application/json
queryParams:
  key: fake-api-key
body:
  type: text
  content: |-
    {
      "email": "test@test.com",
      "password": "geslo123",
      "returnSecureToken": true
    }
scripts:
  - type: afterResponse
    code: |-
      var r = pm.response.json();
      if (r.idToken) { pm.collectionVariables.set('TOKEN', r.idToken); pm.test('Token shranjen', () => pm.expect(r.idToken).to.be.a('string')); }
    language: text/javascript
order: 2000
